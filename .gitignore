# Nix build output
result
result-*

# direnv cache
.direnv/

# Zola build output (flake builds to `result`; `public/` is zola's default)
public/

# OpenCode session storage
.slim/

# Claude Code machine-local settings (permission allowlists, absolute paths)
.claude/settings.local.json

# PragmataPro — proprietary font, must not be committed to public repo
# (FSD license §5.2 redistribution + §7.1 access-control requirement).
# Local copies live here for dev only; use local-fallback CSS pattern.
static/fonts/pragmata/

# Sabon Next — proprietary Linotype font, must not be committed to public repo.
# Local copies live here for dev only; served from R2 (fonts.finnrut.is) in prod.
static/fonts/sabon/