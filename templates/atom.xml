<?xml version="1.0" encoding="UTF-8"?>
{#- Keep the XML declaration on line 1. Tera's trailing whitespace-trim marker
    is honoured on comments anywhere except at the very start of a template, so
    a comment above the declaration leaves a newline behind, pushes it off byte
    0, and every feed reader rejects the result as malformed.

    Atom feed. Adapted from Zola's built-in templates/atom.xml, with three
    changes: the channel title drops the " - Home" suffix the built-in appends
    from the section title, tag feeds are titled "#tag" to match the tag pages,
    and entries carry their tags as categories. Entry bodies stay summary-first
    (every post here sets `<!-- more -->`), so the feed mirrors the homepage.

    Rendered for the homepage section and for every tag; `term` is only defined
    in the latter. Tera autoescapes .xml templates, so bare `{{ }}` is safe and
    only pre-escaped URLs need `| safe`. -#}
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{{ config.title }}{%- if term %} — #{{ term.name }}{%- endif %}</title>
    {%- if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {%- endif %}
    <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
    <link rel="alternate" type="text/html" href="
      {%- if term -%}
        {{ term.permalink | escape_xml | safe }}
      {%- elif section -%}
        {{ section.permalink | escape_xml | safe }}
      {%- else -%}
        {{ config.base_url | escape_xml | safe }}
      {%- endif -%}
    "/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    {% if last_updated is defined %}<updated>{{ last_updated | date(format="%+") }}</updated>{% endif %}
    <id>{{ feed_url | safe }}</id>
    {%- for page in pages %}
    <entry xml:lang="{{ page.lang }}">
        <title>{{ page.title }}</title>
        <published>{{ page.date | date(format="%+") }}</published>
        <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
        <author>
          <name>{% if page.authors %}{{ page.authors[0] }}{% else %}{{ config.author }}{% endif %}</name>
          {%- if config.extra.author_email %}
          <email>{{ config.extra.author_email }}</email>
          {%- endif %}
          {%- if config.extra.author_url %}
          <uri>{{ config.extra.author_url | escape_xml | safe }}</uri>
          {%- endif %}
        </author>
        <link rel="alternate" type="text/html" href="{{ page.permalink | escape_xml | safe }}"/>
        <id>{{ page.permalink | escape_xml | safe }}</id>
        {%- for tag in page.taxonomies.tags | default(value=[]) %}
        <category term="{{ tag }}"/>
        {%- endfor %}
        {%- if page.summary %}
        <summary type="html">{{ page.summary }}</summary>
        {%- else %}
        <content type="html" xml:base="{{ page.permalink | escape_xml | safe }}">{{ page.content }}</content>
        {%- endif %}
    </entry>
    {%- endfor %}
</feed>
